% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple.R
\name{svySE_simple}
\alias{svySE_simple}
\title{Calculate simple indicator tables}
\usage{
svySE_simple(
  data,
  indicators,
  group_vars,
  group_labels = group_vars,
  division = NULL,
  weight = NULL,
  output = c("unweighted", "weighted", "both"),
  target = 1,
  valid_values = c(0, 1),
  pct_mult = 100,
  na_rm = TRUE,
  verbose = TRUE,
  strict = FALSE
)
}
\arguments{
\item{data}{Base de datos previamente cargada en R.}

\item{indicators}{Vector de texto con los nombres de los indicadores.}

\item{group_vars}{Variables utilizadas para agrupar los resultados.}

\item{group_labels}{Etiquetas de las variables de agrupacion.}

\item{division}{Variable opcional para generar resultados separados por
categoria. Si es \code{NULL}, solo se calcula el total.}

\item{weight}{Variable de peso utilizada para calcular frecuencias
expandidas. Es obligatoria cuando \code{output = "weighted"} o
\code{output = "both"}.}

\item{output}{Tipo de resultado: \code{"unweighted"}, \code{"weighted"} o \code{"both"}.}

\item{target}{Valor que identifica la categoria de interes.}

\item{valid_values}{Valores permitidos en los indicadores.}

\item{pct_mult}{Multiplicador utilizado para expresar los porcentajes.}

\item{na_rm}{Si es \code{TRUE}, excluye valores perdidos del indicador.}

\item{verbose}{Si es \code{TRUE}, muestra el avance del calculo.}

\item{strict}{Si es \code{TRUE}, detiene el calculo ante valores no permitidos.}
}
\value{
Objeto de clase \code{"svySE_simple_result"}.
}
\description{
Calcula frecuencias y porcentajes simples, frecuencias expandidas o ambos
tipos de resultados para uno o mas indicadores, sin calcular errores
muestrales.
}
\details{
Calculates unweighted frequencies and percentages, weighted frequencies, or
both types of results for one or more indicators, without calculating
sampling errors.

Con \code{output = "unweighted"} se calculan \code{freq_0}, \code{pct_0}, \code{freq_1},
\code{pct_1}, \code{freq_total} y \code{pct_total}.

Con \code{output = "weighted"} se calculan \code{exp_0}, \code{exp_1} y \code{exp_total}.
Con \code{output = "both"} se calculan ambos conjuntos de columnas.

Los registros con peso perdido no aportan a las frecuencias expandidas,
pero permanecen en las frecuencias sin expandir cuando \code{output = "both"}.
}

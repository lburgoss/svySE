% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple.R
\name{svySE_simple}
\alias{svySE_simple}
\title{Calculate unweighted simple indicator tables}
\usage{
svySE_simple(
  data,
  indicators,
  group_vars,
  group_labels = group_vars,
  division = NULL,
  target = 1,
  valid_values = c(0, 1),
  pct_mult = 100,
  na_rm = TRUE,
  verbose = TRUE,
  strict = FALSE
)
}
\arguments{
\item{data}{Base de datos previamente cargada en R.
Data frame previously loaded in R.}

\item{indicators}{Vector de texto con los nombres de los indicadores.
Character vector containing the indicator names.}

\item{group_vars}{Variables utilizadas para agrupar los resultados.
Variables used to group the results.}

\item{group_labels}{Etiquetas de las variables de agrupacion.
Labels for the grouping variables.}

\item{division}{Variable opcional para generar resultados separados por
categoria. Si es \code{NULL}, solo se calcula el total.
Optional variable used to generate results by category. If \code{NULL}, only
the total is calculated.}

\item{target}{Valor que identifica la categoria de interes.
Value identifying the target category.}

\item{valid_values}{Valores permitidos en los indicadores.
Values allowed in the indicators.}

\item{pct_mult}{Multiplicador utilizado para expresar los porcentajes.
Multiplier used to express percentages.}

\item{na_rm}{Si es \code{TRUE}, excluye los valores perdidos del indicador antes
del calculo y omite los grupos que no contienen ningun registro valido para
ese indicador. Si es \code{FALSE}, la funcion se detiene cuando el indicador
contiene valores perdidos.
If \code{TRUE}, missing indicator values are excluded before calculation and
groups without valid records for that indicator are omitted. If \code{FALSE},
the function stops when the indicator contains missing values.}

\item{verbose}{Si es \code{TRUE}, muestra el avance del calculo.
If \code{TRUE}, displays calculation progress.}

\item{strict}{Si es \code{TRUE}, detiene el calculo cuando encuentra valores fuera
de \code{valid_values}. Si es \code{FALSE}, los excluye con una advertencia.
If \code{TRUE}, stops when values outside \code{valid_values} are found. If \code{FALSE},
they are excluded with a warning.}
}
\value{
Objeto de clase \code{"svySE_simple_result"}.
Object of class \code{"svySE_simple_result"}.
}
\description{
Calcula frecuencias y porcentajes simples para uno o mas indicadores sin
utilizar factores de expansion ni variables del diseno muestral.
}
\details{
Calculates unweighted frequencies and percentages for one or more indicators
without using sampling weights or survey design variables.

Los resultados generados por esta funcion describen solamente los registros
observados en la muestra. Como no se utilizan factores de expansion ni
componentes del diseno muestral, los porcentajes no deben interpretarse como
estimaciones representativas de la poblacion.

Cuando \code{na_rm = TRUE}, los valores perdidos del indicador no forman parte del
denominador. Ademas, los grupos que solo contienen valores perdidos o valores
fuera de \code{valid_values} para un indicador se omiten de la tabla de ese
indicador.

The results generated by this function describe only the records observed in
the sample. Because sampling weights and survey design components are not
used, percentages should not be interpreted as population estimates.

When \code{na_rm = TRUE}, missing indicator values are excluded from the
denominator. Groups containing only missing values or values outside
\code{valid_values} for an indicator are omitted from that indicator table.
}
